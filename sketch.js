function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background('#8a7bc1');

  circle(height/2, width/2, 100, 100);
  textAlign(CENTER);
  text('Weather Control');
  
  
}
